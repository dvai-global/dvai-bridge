android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
org.gradle.jvmargs=-Xmx4096m -XX:+UseG1GC
# Phase 2 Task 3: example apps default to compileSdk 35 (matches the
# SDK's Windows fallback). Override per-host with `-PcompileSdkOverride=36`.
compileSdkOverride=35
