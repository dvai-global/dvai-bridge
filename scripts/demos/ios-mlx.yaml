name: ios-mlx-example
description: examples/ios-mlx — SwiftUI app, MLX backend, mlx-community/Llama-3.2-3B-Instruct-4bit. Apple Silicon only.
output: docs/marketing/assets/ios-mlx-example.mp4
fps: 30
scenes:
  - duration: 5
    caption: "Open examples/ios-mlx in Xcode (Apple Silicon Mac required); SwiftUI app links DVAIBridge + MacPaw OpenAI."
  - duration: 6
    caption: "Pick `iPhone 16` simulator destination on the M-series Mac; build + run."
  - duration: 18
    caption: "Tap 'Load + Ask' — mlx-swift-lm pulls Llama-3.2-3B-Instruct-4bit (~1.8 GB) from HuggingFace Hub."
  - duration: 6
    caption: "MLX loads the 4-bit checkpoint into Apple Silicon GPU memory."
  - duration: 10
    caption: "MacPaw OpenAI SDK streams the chat completion; tokens render in the scroll view."
  - duration: 5
    caption: "Closing card: 'iOS native + MLX + OpenAI Swift SDK on loopback (Apple Silicon)'."
