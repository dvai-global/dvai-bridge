
# Internal-only docs — kept on disk for the author's reference,
# excluded from origin. Removed from history on 2026-05-15.
/PUBLISH-PAPER.md
/RESEARCH.md
/TODO.md
/RELEASE-CHECKLIST.md
/NEXT-RELEASE.md
/scripts/build-research-pdf.mjs
/docs/research/
/docs/superpowers/
/dvai-swarm/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# Misc
.DS_Store
Thumbs.db



# Capacitor
ios/App/public/
!ios/App/public/native-bridge.js
android/app/src/main/assets/public
!android/app/src/main/assets/public/native-bridge.js
resources/android/icon
resources/android/splash
resources/ios/icon
resources/ios/splash
resources/windows/icon
resources/windows/splash

# Production build
www/
dist/
packages/*/dist/
packages/*/README.md
packages/*/LICENSE
# .NET NuGet packages need a real README + LICENSE inside the .nupkg
# (referenced by <PackageReadmeFile> / <PackageLicenseFile> at pack time).
# Track these explicitly; the global packages/*/README.md ignore would
# otherwise hide future edits from `git status`.
!packages/dvai-bridge-dotnet/README.md
# Flutter / pub.dev honors .gitignore at publish time, so the global
# packages/*/LICENSE + README.md ignore was hiding both files from the
# uploaded archive — `dart pub publish` then refused with "You must have
# a LICENSE file in the root directory." Track them explicitly.
!packages/dvai-bridge-flutter/LICENSE
!packages/dvai-bridge-flutter/README.md
example/dist/
example/*/dist/
.vite/
RESEARCH.pdf
RESEARCH.html

# VitePress build artifacts
docs/.vitepress/cache/
docs/.vitepress/.temp/
docs/.vitepress/dist/

# Claude Code local config
.claude/

# Git worktrees (local-only)
.worktrees/

# Mac-remote-build local config (never committed; per-developer)
scripts/mac.local.json
scripts/*.local.json
scripts/*.local.env
.env.local
CLAUDE.md

# Xcode + SwiftPM build output — disposable, regenerated on every build.
# The bare `.build/` pattern catches SwiftPM caches anywhere in the tree
# (packages/<x>/.build, packages/<x>/ios/.build, examples/<x>/.build, etc.)
.build/
packages/*/ios/build/

# Swift Package Manager lockfile — for a library, consumers resolve their own.
# Bare pattern catches it at any package or example root.
Package.resolved

# Android Gradle build output — disposable, regenerated on every build
packages/*/android/.gradle/
packages/*/android/build/
packages/*/android/local.properties
# `.cxx/` is the AGP-9 NDK build dir; Phase 2 Task 3 surfaced it because
# llama-core triggers a clean compile when the example apps publish to
# mavenLocal on a fresh host.
packages/*/android/.cxx/

# Example-app Gradle output (Phase 2 examples matrix).
examples/*/.gradle/
examples/*/build/
examples/*/app/build/
examples/*/app/.cxx/
examples/*/local.properties
examples/*/.idea/

# Marketing scripts + blog drafts — private, not for the public repo
docs/marketing/

# Per-developer publishing checklist — not for the public repo (contains
# tokens, manual gotchas, and registry-specific quirks the user maintains).
PUBLISHING.md

# Rendezvous-server referral-link setup — private, contains the project
# owner's affiliate codes for the one-click deploy buttons. Do NOT commit.
RENDEZVOUS-REFERRALS.md

# CocoaPods — pod install / pod lib lint output (per-developer, regenerated)
**/Pods/

# Xcode build output — DerivedData (with custom -derivedDataPath inside repo)
# and per-user state inside .xcodeproj bundles
**/DerivedData/
**/xcuserdata/
*.xcuserstate

# llama.cpp xcframework build output — explicitly allowed for CocoaPods release
!packages/*/android/src/main/cpp/native/*/build-apple/*.xcframework
!packages/*/android/src/main/cpp/native/*/build-apple/**/*.xcframework

# Pod-local copy of the xcframeworks — allowed for CocoaPods release
!packages/dvai-bridge-ios/Frameworks/
!packages/dvai-bridge-ios/Frameworks/*.xcframework
!packages/dvai-bridge-ios/Frameworks/**/*.xcframework

# Pod-local mirror of sibling *-core source dirs — populated by
# DVAIBridge.podspec's prepare_command. Source of truth lives in the
# real *-core packages.
packages/dvai-bridge-ios/Sources/_external/

# Android Studio / IntelliJ — per-developer
.idea/
*.iml
experiments/mlc-perf-spike/

# Local cross-platform validation helpers (machine-specific paths).
/mac-build.sh
/mac-probe.sh
/mac-rust-update.sh
/mac-setup.sh
/wsl-build.sh
/wsl-setup.sh
/mac-build.log
/mac-setup.log
/wsl-build.log
/mac-toolchain-install.log
/hub/tauri-build.log
/MORNING-REVIEW.md

# License smoke-test artifacts — never commit these. The JWT is signed
# with the live production private key; the smoke script is a one-off
# verification helper.
/dvai-license.jwt
/smoke-license.mjs

# Local-only release artifacts built by scripts/prepare-ios-release.sh.
# These are uploaded to the GitHub Release manually; never tracked.
/DVAIBridge-v*.zip