name: Android JVM — litert-core

on:
  pull_request:
    paths:
      - 'packages/dvai-bridge-android-litert-core/**'
      - 'packages/dvai-bridge-android-shared-core/**'
      - '.github/workflows/test-android-litert-core-jvm.yml'
  push:
    branches:
      - main
    paths:
      - 'packages/dvai-bridge-android-litert-core/**'
      - 'packages/dvai-bridge-android-shared-core/**'

concurrency:
  group: android-litert-core-jvm-${{ github.ref }}
  cancel-in-progress: true

jobs:
  jvm-test:
    name: android-litert-core JVM
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: packages/dvai-bridge-android-litert-core/android
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-litert-core-${{ hashFiles('packages/dvai-bridge-android-shared-core/android/**/*.gradle*', 'packages/dvai-bridge-android-litert-core/android/**/*.gradle*', 'packages/dvai-bridge-android-litert-core/android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-litert-core-

      - name: Publish android-shared-core to mavenLocal
        working-directory: packages/dvai-bridge-android-shared-core/android
        run: ./gradlew publishToMavenLocal --no-daemon --stacktrace

      - run: ./gradlew testDebugUnitTest --no-daemon --stacktrace

      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-litert-core-reports
          path: packages/dvai-bridge-android-litert-core/android/build/reports/
          retention-days: 14
          if-no-files-found: ignore
