name: Android JVM — capacitor-llama

on:
  pull_request:
    paths:
      - 'packages/dvai-bridge-capacitor-llama/android/**'
      - 'packages/dvai-bridge-capacitor/src/**'
      - 'fixtures/**'
      - '.github/workflows/test-android-llama-jvm.yml'
  push:
    branches:
      - main
    paths:
      - 'packages/dvai-bridge-capacitor-llama/android/**'
      - 'packages/dvai-bridge-capacitor/src/**'
      - 'fixtures/**'

concurrency:
  group: android-llama-jvm-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gradle-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: packages/dvai-bridge-capacitor-llama/android
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-llama-${{ hashFiles('packages/dvai-bridge-capacitor-llama/android/**/*.gradle*', 'packages/dvai-bridge-capacitor-llama/android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-llama-

      - name: Gradle test
        run: ./gradlew test --no-daemon --stacktrace

      - name: Upload reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: android-llama-reports
          path: packages/dvai-bridge-capacitor-llama/android/build/reports/
          retention-days: 7
