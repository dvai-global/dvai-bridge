// Web-build stub for `audience_io.dart`. On web, `dart:io` doesn't
// exist, so there's no process-environment to read — every lookup
// returns null and the validator falls through to its platform-
// default detection branches.

/// Stub for the dart:io environment lookup. Always returns null on web.
String? environment(String name) => null;
