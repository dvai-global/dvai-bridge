# Standard Android Gradle properties for a library module.
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
org.gradle.jvmargs=-Xmx4096m

# Phase 3D umbrella AAR version this plugin consumes. Pinned to the
# currently-published cores; Task 14 bumps every dvai-bridge module
# (cores + this plugin) together via scripts/sync-versions.js.
dvaiBridgeVersion=3.0.0
