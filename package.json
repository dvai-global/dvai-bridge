{
  "name": "dvai-bridge-monorepo",
  "private": true,
  "version": "1.5.1",
  "description": "Monorepo for DVAI-Bridge: Local AI Orchestration",
  "author": "Deep Chakraborty <https://github.com/dk013>",
  "license": "Custom (See LICENSE)",
  "type": "module",
  "scripts": {
    "prepare": "pnpm run build",
    "prebuild": "node scripts/sync-versions.js",
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "vitest run",
    "build:research": "pnpm add -D -w marked && node scripts/build-research-pdf.mjs; pnpm remove -w marked"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "cross-env": "^10.1.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}